apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: sre-lab-hpa
spec:
  scaleTargetRef:
    apiVersion: app/v1
    kind: Deployment
    name: sre-api
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
